import React, { memo } from 'react';
import { withNativeProps } from '../../utils/native-props';
import classNames from 'classnames';
import { mergeProps } from '../../utils/with-default-props';
var defaultProps = {
  color: 'primary'
};
export var PageIndicator = /*#__PURE__*/memo(function (p) {
  var props = mergeProps(defaultProps, p);
  var dots = [];

  for (var i = 0; i < props.total; i++) {
    dots.push( /*#__PURE__*/React.createElement("div", {
      key: i,
      className: classNames('adm-page-indicator-dot', {
        'adm-page-indicator-dot-active': props.current === i
      })
    }));
  }

  return withNativeProps(props, /*#__PURE__*/React.createElement("div", {
    className: classNames('adm-page-indicator', "adm-page-indicator-color-" + props.color)
  }, dots));
});