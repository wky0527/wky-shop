import { FC } from 'react';
declare type Props = {
    image: string;
    maxZoom: number;
    onTap: () => void;
    onZoomChange?: (zoom: number) => void;
};
export declare const Slide: FC<Props>;
export {};
